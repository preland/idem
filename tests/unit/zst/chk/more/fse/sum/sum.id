// Column sums over a whole table.

t_fsum(int base, int n) {
  int a = t_fcol((import fse_sy), base, n);
  t_fsum2(a, base, n);
} return void;

t_fshow(int a, int b, int c) {
  print("    sums sym " + a + " nb " + b + " base " + c);
} return void;

t_fcol(int[] xs, int base, int n) {
  int v = 0;
  int i = 0;
  while (i < n) {
    v = v + xs[base + i];
    i = i + 1;
  }
} return int v;
