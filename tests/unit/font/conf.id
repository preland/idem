// The font decoder opens a file, so it needs the engine it lives in; the file
// backend is idstd's.
import "../../../engine"
// The game seam as no-ops: this program links the engine but is not a game.
import "../../../stub"
