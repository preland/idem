// The sprite loader needs the file backend (it opens a PNG), and the window seam
// because engine/core/sys names gfx_* whether or not this test calls one.
import "../../../engine"
// The game seam as no-ops: this program links the engine but is not a game.
import "../../../stub"
import "../../../../id_development/backends/gfx"
import "../../../../id_development/backends/fs"
