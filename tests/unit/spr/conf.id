// The sprite loader needs the file backend (it opens a PNG); fs is idstd's.
import "../../../engine"
// The game seam as no-ops: this program links the engine but is not a game.
import "../../../stub"
