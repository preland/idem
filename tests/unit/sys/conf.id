// The whole engine. engine/core/sys/ names gfx_open, gfx_poll and gfx_present,
// which idstd declares; nothing in this test reaches one -- the viewport
// arithmetic and the key-state machine are pure -- so no backend is linked and
// it runs with no display, in CI, like every other unit suite.
import "../../../engine"
// The game seam as no-ops: this program links the engine but is not a game.
import "../../../stub"
