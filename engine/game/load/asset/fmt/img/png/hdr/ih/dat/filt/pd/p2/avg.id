// Average and Paeth -- the two filters that look at both neighbours.

png_pred3(int y, int r, int i, int f) {
  int v = 0;
  if (f = 3) { v = (png_left(r, i) + png_up(y, i)) / 2; }
  else if (f = 4) { v = png_paeth(png_left(r, i), png_up(y, i), png_ul(y, i)); }
} return int v;
