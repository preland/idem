// The Z Euler angle, read from the SDNA field and converted to millidegrees.

bl_wrot_deg(int p, int si) {
  int bl_foff_v = bl_foff(si, "rot[3]");
  int bl_f32_v = bl_f32(p + bl_foff_v + 8);
  int bl_deg_v = bl_deg(bl_f32_v);
} return int bl_deg_v;
