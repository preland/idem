// Helper for huf_last2 setup.

huf_last2_setup(int rest, int n) {
  int fse_hib_v = fse_hib(rest);
  lset((import huf_rk), fse_hib_v + 1, (import huf_rk)[fse_hib_v + 1] + 1);
  lset((import huf_st), 1, n + 1);
} return void;
