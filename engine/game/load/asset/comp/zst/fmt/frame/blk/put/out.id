// A byte already written, addressed backwards from the cursor, and the result.

zst_back(int d) {
} return int peek8((import zst_st)[3] + (import zst_st)[4] - d);

zst_out() {
  int n = (import zst_st)[4];
  if ((import zst_st)[6] != 0) { n = 0 - 1; }
} return int n;
