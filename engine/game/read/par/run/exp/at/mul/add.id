par_add(int[] pos) {
  int lv = par_mul(pos);
  while (par_is_addop(pos)) {
    lv = par_add_f(pos, lv);
  }
} return int lv;

par_add_f(int[] pos, int lv) {
  string txt = par_optxt(pos);
  int nd = par_mul(pos);
  int ret_i = par_binnode(txt, lv, nd);
} return int ret_i;
