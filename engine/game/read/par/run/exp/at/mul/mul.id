par_mul(int[] pos) {
  int lv = par_unary(pos);
  while (par_is_mulop(pos)) {
    lv = par_mul_f(pos, lv);
  }
} return int lv;

par_mul_f(int[] pos, int lv) {
  string txt = par_optxt(pos);
  int nd = par_unary(pos);
  int ret_i = par_binnode(txt, lv, nd);
} return int ret_i;
