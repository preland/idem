par_shift(int[] pos) {
  int left = par_add(pos);
  while (par_is_shop(pos)) {
    left = par_shift_f(pos, left);
  }
} return int left;

par_shift_f(int[] pos, int left) {
  string txt = par_text(pos);
  par_next(pos);
  int nd = par_add(pos);
} return int ast_new_ab(ast_k_bin(), txt, left, nd);
