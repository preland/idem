par_shift(int[] pos) {
  int lv = par_add(pos);
  while (par_is_shop(pos)) {
    lv = par_shift_f(pos, lv);
  }
} return int lv;

par_shift_f(int[] pos, int lv) {
  string txt = par_optxt(pos);
  int nd = par_add(pos);
  int ret_i = par_binnode(txt, lv, nd);
} return int ret_i;
