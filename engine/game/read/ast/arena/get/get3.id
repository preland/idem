ast_lnof(int nd) {
  int ln = (import ast_line)[nd];
} return int ln;

ast_n() {
  int cnt = len((import ast_kind));
} return int cnt;

ast_kname(int kd) {
  string nm = (import ast_kn)[kd - (import ast_kbase)];
} return string nm;
