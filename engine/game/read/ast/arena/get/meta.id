ast_lnof(int nd) {
  int ln = (import ast_line)[nd];
} return int ln;

ast_n() {
  int n = len((import ast_kind));
} return int n;

ast_kname(int kd) {
  string nm = (import ast_kn)[kd - (import ast_kbase)];
} return string nm;
